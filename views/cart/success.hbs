<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/css/home.css">
    <style>
        .success-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .success-card {
            background: white;
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.1);
            text-align: center;
            max-width: 500px;
        }
        
        .success-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            animation: bounce 2s infinite;
        }
        
        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }
        
        .success-card h1 {
            color: #333;
            margin-bottom: 1rem;
        }
        
        .success-card p {
            color: #666;
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }
        
        .order-info {
            background: #f8f9ff;
            padding: 1.5rem;
            border-radius: 10px;
            margin: 2rem 0;
        }
        
        .order-info h3 {
            color: #333;
            margin-bottom: 1rem;
        }
        
        .btn-home {
            display: inline-block;
            padding: 0.75rem 2rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            text-decoration: none;
            border-radius: 10px;
            font-weight: 600;
            transition: transform 0.2s;
        }
        
        .btn-home:hover {
            transform: translateY(-2px);
        }
        
        .btn-shop {
            display: inline-block;
            padding: 0.75rem 2rem;
            background: white;
            color: #667eea;
            text-decoration: none;
            border: 2px solid #667eea;
            border-radius: 10px;
            font-weight: 600;
            margin-left: 1rem;
            transition: all 0.2s;
        }
        
        .btn-shop:hover {
            background: #667eea;
            color: white;
        }
    </style>
</head>
<body>
    <div class="success-container">
        <div class="success-card">
            <div class="success-icon">✅</div>
            <h1>¡Pago Exitoso!</h1>
            <p>{{message}}</p>
            <p>Tu pedido ha sido procesado correctamente y recibirás un correo de confirmación pronto.</p>
            
            <div class="order-info">
                <h3>📋 Información del Pedido</h3>
                <p><strong>Número de Orden:</strong> {{orderId}}</p>
                <p><strong>Estado:</strong> Procesando</p>
                <p><strong>Tiempo estimado de entrega:</strong> 3-5 días hábiles</p>
            </div>
            
            <div>
                <a href="/" class="btn-home">🏠 Volver al Inicio</a>
                <a href="/shop" class="btn-shop">🛒 Seguir Comprando</a>
            </div>
        </div>
    </div>
</body>
</html>
